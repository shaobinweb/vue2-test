<view class="container">
<!-- 分类 -->
  <view class='btn-area'>
    <scroll-view class='scroll-view_H' scroll-x="true" style='width:100%;'>
      <text wx:for="{{order}}" wx:key='id' data-id="{{item.id}}" bindtap='ClickScroll' class="nav-name {{item.id == currentId ? 'nav-hover' : ''}}" >{{item.title}}</text>
    </scroll-view>
  </view> 
</view>
